// This file was auto-generated by Fern from our API Definition.

import type { Artifact } from "./Artifact.js";
import type { Call } from "./Call.js";
import type { Chat } from "./Chat.js";
import type { CreateAssistantDto } from "./CreateAssistantDto.js";
import type { CreateCustomerDto } from "./CreateCustomerDto.js";
import type { OpenAiMessage } from "./OpenAiMessage.js";
import type { ServerMessageCallEndpointingRequestMessagesItem } from "./ServerMessageCallEndpointingRequestMessagesItem.js";
import type { ServerMessageCallEndpointingRequestPhoneNumber } from "./ServerMessageCallEndpointingRequestPhoneNumber.js";
import type { ServerMessageCallEndpointingRequestType } from "./ServerMessageCallEndpointingRequestType.js";

export interface ServerMessageCallEndpointingRequest {
    /** This is the phone number that the message is associated with. */
    phoneNumber?: ServerMessageCallEndpointingRequestPhoneNumber | undefined;
    /**
     * This is the type of the message. "call.endpointing.request" is sent when using `assistant.startSpeakingPlan.smartEndpointingPlan={ "provider": "custom-endpointing-model" }`.
     *
     * Here is what the request will look like:
     *
     * POST https://{assistant.startSpeakingPlan.smartEndpointingPlan.server.url}
     * Content-Type: application/json
     *
     * {
     *   "message": {
     *     "type": "call.endpointing.request",
     *     "messages": [
     *       {
     *         "role": "user",
     *         "message": "Hello, how are you?",
     *         "time": 1234567890,
     *         "secondsFromStart": 0
     *       }
     *     ],
     *     ...other metadata about the call...
     *   }
     * }
     *
     * The expected response:
     * {
     *   "timeoutSeconds": 0.5
     * }
     */
    type: ServerMessageCallEndpointingRequestType;
    /** This is the conversation history at the time of the endpointing request. */
    messages?: ServerMessageCallEndpointingRequestMessagesItem[] | undefined;
    /** This is just `messages` formatted for OpenAI. */
    messagesOpenAIFormatted: OpenAiMessage[];
    /** This is the timestamp of the message. */
    timestamp?: number | undefined;
    /**
     * This is a live version of the `call.artifact`.
     *
     * This matches what is stored on `call.artifact` after the call.
     */
    artifact?: Artifact | undefined;
    /** This is the assistant that the message is associated with. */
    assistant?: CreateAssistantDto | undefined;
    /** This is the customer that the message is associated with. */
    customer?: CreateCustomerDto | undefined;
    /** This is the call that the message is associated with. */
    call?: Call | undefined;
    /** This is the chat object. */
    chat?: Chat | undefined;
}
