// This file was auto-generated by Fern from our API Definition.

import type { CustomLlmCredentialProvider } from "./CustomLlmCredentialProvider.js";
import type { OAuth2AuthenticationPlan } from "./OAuth2AuthenticationPlan.js";
import type { Oauth2AuthenticationSession } from "./Oauth2AuthenticationSession.js";

export interface CustomLlmCredential {
    provider: CustomLlmCredentialProvider;
    /** This is not returned in the API. */
    apiKey: string;
    /** This is the authentication plan. Currently supports OAuth2 RFC 6749. To use Bearer authentication, use apiKey */
    authenticationPlan?: OAuth2AuthenticationPlan | undefined;
    /** This is the unique identifier for the credential. */
    id: string;
    /** This is the unique identifier for the org that this credential belongs to. */
    orgId: string;
    /** This is the ISO 8601 date-time string of when the credential was created. */
    createdAt: string;
    /** This is the ISO 8601 date-time string of when the assistant was last updated. */
    updatedAt: string;
    /** This is the authentication session for the credential. Available for credentials that have an authentication plan. */
    authenticationSession?: Oauth2AuthenticationSession | undefined;
    /** This is the name of credential. This is just for your reference. */
    name?: string | undefined;
}
