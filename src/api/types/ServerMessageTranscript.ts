// This file was auto-generated by Fern from our API Definition.

import type { Artifact } from "./Artifact.js";
import type { Call } from "./Call.js";
import type { Chat } from "./Chat.js";
import type { CreateAssistantDto } from "./CreateAssistantDto.js";
import type { CreateCustomerDto } from "./CreateCustomerDto.js";
import type { ServerMessageTranscriptPhoneNumber } from "./ServerMessageTranscriptPhoneNumber.js";
import type { ServerMessageTranscriptRole } from "./ServerMessageTranscriptRole.js";
import type { ServerMessageTranscriptTranscriptType } from "./ServerMessageTranscriptTranscriptType.js";
import type { ServerMessageTranscriptType } from "./ServerMessageTranscriptType.js";

export interface ServerMessageTranscript {
    /** This is the phone number that the message is associated with. */
    phoneNumber?: ServerMessageTranscriptPhoneNumber | undefined;
    /** This is the type of the message. "transcript" is sent as transcriber outputs partial or final transcript. */
    type: ServerMessageTranscriptType;
    /** This is the timestamp of the message. */
    timestamp?: number | undefined;
    /**
     * This is a live version of the `call.artifact`.
     *
     * This matches what is stored on `call.artifact` after the call.
     */
    artifact?: Artifact | undefined;
    /** This is the assistant that the message is associated with. */
    assistant?: CreateAssistantDto | undefined;
    /** This is the customer that the message is associated with. */
    customer?: CreateCustomerDto | undefined;
    /** This is the call that the message is associated with. */
    call?: Call | undefined;
    /** This is the chat object. */
    chat?: Chat | undefined;
    /** This is the role for which the transcript is for. */
    role: ServerMessageTranscriptRole;
    /** This is the type of the transcript. */
    transcriptType: ServerMessageTranscriptTranscriptType;
    /** This is the transcript content. */
    transcript: string;
    /** Indicates if the transcript was filtered for security reasons. */
    isFiltered?: boolean | undefined;
    /** List of detected security threats if the transcript was filtered. */
    detectedThreats?: string[] | undefined;
    /** The original transcript before filtering (only included if content was filtered). */
    originalTranscript?: string | undefined;
}
