// This file was auto-generated by Fern from our API Definition.

import type { Call } from "./Call.js";
import type { ClientMessageUserInterruptedPhoneNumber } from "./ClientMessageUserInterruptedPhoneNumber.js";
import type { ClientMessageUserInterruptedType } from "./ClientMessageUserInterruptedType.js";
import type { CreateAssistantDto } from "./CreateAssistantDto.js";
import type { CreateCustomerDto } from "./CreateCustomerDto.js";

export interface ClientMessageUserInterrupted {
    /** This is the phone number that the message is associated with. */
    phoneNumber?: ClientMessageUserInterruptedPhoneNumber | undefined;
    /** This is the type of the message. "user-interrupted" is sent when the user interrupts the assistant. */
    type: ClientMessageUserInterruptedType;
    /**
     * This is the turnId of the LLM response that was interrupted. Matches the turnId
     * on model-output messages so clients can discard the interrupted turn's tokens.
     */
    turnId?: string | undefined;
    /** This is the timestamp of the message. */
    timestamp?: number | undefined;
    /** This is the call that the message is associated with. */
    call?: Call | undefined;
    /** This is the customer that the message is associated with. */
    customer?: CreateCustomerDto | undefined;
    /** This is the assistant that the message is associated with. */
    assistant?: CreateAssistantDto | undefined;
}
