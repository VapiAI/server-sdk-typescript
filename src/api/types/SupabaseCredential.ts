// This file was auto-generated by Fern from our API Definition.

import type { SupabaseBucketPlan } from "./SupabaseBucketPlan.js";
import type { SupabaseCredentialProvider } from "./SupabaseCredentialProvider.js";

export interface SupabaseCredential {
    /** This is for supabase storage. */
    provider: SupabaseCredentialProvider;
    /** This is the order in which this storage provider is tried during upload retries. Lower numbers are tried first in increasing order. */
    fallbackIndex?: number | undefined;
    /** This is the unique identifier for the credential. */
    id: string;
    /** This is the unique identifier for the org that this credential belongs to. */
    orgId: string;
    /** This is the ISO 8601 date-time string of when the credential was created. */
    createdAt: string;
    /** This is the ISO 8601 date-time string of when the assistant was last updated. */
    updatedAt: string;
    /** This is the name of credential. This is just for your reference. */
    name?: string | undefined;
    bucketPlan?: SupabaseBucketPlan | undefined;
}
