// This file was auto-generated by Fern from our API Definition.

import type { Artifact } from "./Artifact.js";
import type { Call } from "./Call.js";
import type { Chat } from "./Chat.js";
import type { CreateAssistantDto } from "./CreateAssistantDto.js";
import type { CreateCustomerDto } from "./CreateCustomerDto.js";
import type { OpenAiMessage } from "./OpenAiMessage.js";
import type { ServerMessageKnowledgeBaseRequestMessagesItem } from "./ServerMessageKnowledgeBaseRequestMessagesItem.js";
import type { ServerMessageKnowledgeBaseRequestPhoneNumber } from "./ServerMessageKnowledgeBaseRequestPhoneNumber.js";
import type { ServerMessageKnowledgeBaseRequestType } from "./ServerMessageKnowledgeBaseRequestType.js";

export interface ServerMessageKnowledgeBaseRequest {
    /** This is the phone number that the message is associated with. */
    phoneNumber?: ServerMessageKnowledgeBaseRequestPhoneNumber | undefined;
    /** This is the type of the message. "knowledge-base-request" is sent to request knowledge base documents. To enable, use `assistant.knowledgeBase.provider=custom-knowledge-base`. */
    type: ServerMessageKnowledgeBaseRequestType;
    /** These are the messages that are going to be sent to the `model` right after the `knowledge-base-request` webhook completes. */
    messages?: ServerMessageKnowledgeBaseRequestMessagesItem[] | undefined;
    /** This is just `messages` formatted for OpenAI. */
    messagesOpenAIFormatted: OpenAiMessage[];
    /** This is the timestamp of the message. */
    timestamp?: number | undefined;
    /**
     * This is a live version of the `call.artifact`.
     *
     * This matches what is stored on `call.artifact` after the call.
     */
    artifact?: Artifact | undefined;
    /** This is the assistant that the message is associated with. */
    assistant?: CreateAssistantDto | undefined;
    /** This is the customer that the message is associated with. */
    customer?: CreateCustomerDto | undefined;
    /** This is the call that the message is associated with. */
    call?: Call | undefined;
    /** This is the chat object. */
    chat?: Chat | undefined;
}
