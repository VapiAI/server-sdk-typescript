// This file was auto-generated by Fern from our API Definition.

import type { Artifact } from "./Artifact.js";
import type { Call } from "./Call.js";
import type { Chat } from "./Chat.js";
import type { CreateAssistantDto } from "./CreateAssistantDto.js";
import type { CreateCustomerDto } from "./CreateCustomerDto.js";
import type { OpenAiMessage } from "./OpenAiMessage.js";
import type { ServerMessageStatusUpdateDestination } from "./ServerMessageStatusUpdateDestination.js";
import type { ServerMessageStatusUpdateEndedReason } from "./ServerMessageStatusUpdateEndedReason.js";
import type { ServerMessageStatusUpdateMessagesItem } from "./ServerMessageStatusUpdateMessagesItem.js";
import type { ServerMessageStatusUpdatePhoneNumber } from "./ServerMessageStatusUpdatePhoneNumber.js";
import type { ServerMessageStatusUpdateStatus } from "./ServerMessageStatusUpdateStatus.js";
import type { ServerMessageStatusUpdateType } from "./ServerMessageStatusUpdateType.js";

export interface ServerMessageStatusUpdate {
    /** This is the phone number that the message is associated with. */
    phoneNumber?: ServerMessageStatusUpdatePhoneNumber | undefined;
    /** This is the type of the message. "status-update" is sent whenever the `call.status` changes. */
    type: ServerMessageStatusUpdateType;
    /** This is the status of the call. */
    status: ServerMessageStatusUpdateStatus;
    /** This is the reason the call ended. This is only sent if the status is "ended". */
    endedReason?: ServerMessageStatusUpdateEndedReason | undefined;
    /** These are the conversation messages of the call. This is only sent if the status is "forwarding". */
    messages?: ServerMessageStatusUpdateMessagesItem[] | undefined;
    /** These are the conversation messages of the call. This is only sent if the status is "forwarding". */
    messagesOpenAIFormatted?: OpenAiMessage[] | undefined;
    /** This is the destination the call is being transferred to. This is only sent if the status is "forwarding". */
    destination?: ServerMessageStatusUpdateDestination | undefined;
    /** This is the timestamp of the message. */
    timestamp?: number | undefined;
    /**
     * This is a live version of the `call.artifact`.
     *
     * This matches what is stored on `call.artifact` after the call.
     */
    artifact?: Artifact | undefined;
    /** This is the assistant that the message is associated with. */
    assistant?: CreateAssistantDto | undefined;
    /** This is the customer that the message is associated with. */
    customer?: CreateCustomerDto | undefined;
    /** This is the call that the message is associated with. */
    call?: Call | undefined;
    /** This is the chat object. */
    chat?: Chat | undefined;
    /** This is the transcript of the call. This is only sent if the status is "forwarding". */
    transcript?: string | undefined;
    /** This is the summary of the call. This is only sent if the status is "forwarding". */
    summary?: string | undefined;
    /**
     * This is the inbound phone call debugging artifacts. This is only sent if the status is "ended" and there was an error accepting the inbound phone call.
     *
     * This will include any errors related to the "assistant-request" if one was made.
     */
    inboundPhoneCallDebuggingArtifacts?: Record<string, unknown> | undefined;
}
