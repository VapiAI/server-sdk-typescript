// This file was auto-generated by Fern from our API Definition.

import type { Oauth2AuthenticationSession } from "./Oauth2AuthenticationSession.js";
import type { WebhookCredentialAuthenticationPlan } from "./WebhookCredentialAuthenticationPlan.js";
import type { WebhookCredentialProvider } from "./WebhookCredentialProvider.js";

export interface WebhookCredential {
    provider: WebhookCredentialProvider;
    /** This is the authentication plan. Supports OAuth2 RFC 6749, HMAC signing, and Bearer authentication. */
    authenticationPlan: WebhookCredentialAuthenticationPlan;
    /** This is the unique identifier for the credential. */
    id: string;
    /** This is the unique identifier for the org that this credential belongs to. */
    orgId: string;
    /** This is the ISO 8601 date-time string of when the credential was created. */
    createdAt: string;
    /** This is the ISO 8601 date-time string of when the assistant was last updated. */
    updatedAt: string;
    /** This is the authentication session for the credential. Available for credentials that have an authentication plan. */
    authenticationSession: Oauth2AuthenticationSession;
    /** This is the name of credential. This is just for your reference. */
    name?: string | undefined;
}
