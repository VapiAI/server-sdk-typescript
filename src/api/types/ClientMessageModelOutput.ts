// This file was auto-generated by Fern from our API Definition.

import type { Call } from "./Call.js";
import type { ClientMessageModelOutputPhoneNumber } from "./ClientMessageModelOutputPhoneNumber.js";
import type { ClientMessageModelOutputType } from "./ClientMessageModelOutputType.js";
import type { CreateAssistantDto } from "./CreateAssistantDto.js";
import type { CreateCustomerDto } from "./CreateCustomerDto.js";

export interface ClientMessageModelOutput {
    /** This is the phone number that the message is associated with. */
    phoneNumber?: ClientMessageModelOutputPhoneNumber | undefined;
    /** This is the type of the message. "model-output" is sent as the model outputs tokens. */
    type: ClientMessageModelOutputType;
    /**
     * This is the unique identifier for the current LLM turn. All tokens from the same
     * LLM response share the same turnId. Use this to group tokens and discard on interruption.
     */
    turnId?: string | undefined;
    /** This is the timestamp of the message. */
    timestamp?: number | undefined;
    /** This is the call that the message is associated with. */
    call?: Call | undefined;
    /** This is the customer that the message is associated with. */
    customer?: CreateCustomerDto | undefined;
    /** This is the assistant that the message is associated with. */
    assistant?: CreateAssistantDto | undefined;
    /** This is the output of the model. It can be a token or tool call. */
    output: Record<string, unknown>;
}
