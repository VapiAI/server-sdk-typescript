// This file was auto-generated by Fern from our API Definition.

import type { ChunkPlan } from "./ChunkPlan.js";
import type { FallbackOpenAiVoiceId } from "./FallbackOpenAiVoiceId.js";
import type { FallbackOpenAiVoiceModel } from "./FallbackOpenAiVoiceModel.js";

export interface FallbackOpenAiVoice {
    /** This is the flag to toggle voice caching for the assistant. */
    cachingEnabled?: boolean | undefined;
    /**
     * This is the provider-specific ID that will be used.
     * Please note that ash, ballad, coral, sage, and verse may only be used with realtime models.
     */
    voiceId: FallbackOpenAiVoiceId;
    /** This is the model that will be used for text-to-speech. */
    model?: FallbackOpenAiVoiceModel | undefined;
    /**
     * This is a prompt that allows you to control the voice of your generated audio.
     * Does not work with 'tts-1' or 'tts-1-hd' models.
     */
    instructions?: string | undefined;
    /** This is the speed multiplier that will be used. */
    speed?: number | undefined;
    /** This is the plan for chunking the model output before it is sent to the voice provider. */
    chunkPlan?: ChunkPlan | undefined;
}
